# Quarantine

Do not add speculative architecture trees, synthetic audit reports, or unverified metrics here.

Ship only code and docs that are tested under `npm test` and `npm run healthcheck`. For product intent, see `ghoststack_dossier.html` and `resource-readme.html` at the repository root.
